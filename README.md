# TileMason

**通用 2D 像素地图拼装桌面工具** —— 像玩《我的世界》创造模式一样，用素材件搭出街道、建筑、庭院与室内。每个件都可编辑、可移动、可删除，可保存为结构化地图数据，并导出为 Godot 场景或 JSON。

*A general-purpose desktop tool for assembling 2D pixel tile maps — place, move and edit every piece like Minecraft creative mode, then export to Godot scenes or structured JSON.*

> 🚧 项目处于 **P1（区域与图层）** 开发阶段：**P0「可用画笔」已完成**（放置/拖刷/直线/矩形填充、橡皮擦、撤销重做、保存载入、图层显隐锁定、地图检查，九条验收全过）。P1 的框选/移动/复制/镜像已就位，图层透明度等在路上。

## 交付形态：独立桌面软件

TileMason 是**独立桌面软件**——最终用户（美术、策划、关卡设计）下载安装包，双击即用，**不需要安装 Godot 或任何依赖**。Godot 4 只是它的开发框架（同类先例：像素画软件 Pixelorama、材质工具 Material Maker）。将来随发布提供 Windows / macOS / Linux 安装包；首个发布版之前可从源码运行（见下方开发者指南）。

## 核心特性（按设计文档推进）

| 特性 | 状态 |
|------|------|
| 画布平移 + 整数倍显示缩放（100%~800%，最近邻像素） | ✅ |
| 正式网格 + 主网格辅助线（默认 16px，可配） | ✅ |
| 自由摆放模式（半透明预览/网格吸附/底边中心锚点/拖刷连放） | ✅ |
| 直线工具 / 矩形填充（覆盖或遇已有停止） | ✅ |
| 橡皮擦（单格/笔刷，只清当前层，整段可撤销） | ✅ |
| 图层栈（显示/隐藏/锁定；透明度/独显/层级调整在路上） | ✅ 部分 |
| 选择模式（框选/单击选/多选移动/复制粘贴/删除/镜像） | ✅ |
| 撤销/重做（区域操作整段撤销） | ✅ |
| 保存/载入地图（自动恢复上次编辑） | ✅ |
| 地图检查（道路连通/建筑堵路） | ✅ 最小版 |
| 连续相接模式（自动连接变体/油漆桶） | 📅 P2 |
| 素材库（搜索/收藏/最近使用/快捷栏） | 📅 P3 |
| 预制件（组合保存/整体复用） | 📅 P3 |
| 导出运行时场景 / 结构化 JSON（形态A） | ✅ |

设计基准文档：[docs/design.md](docs/design.md)（含完整交互约定与第一版验收标准）；上手指南：[docs/user-guide.md](docs/user-guide.md)。

## 快捷键速查

| 操作 | 按键 |
|------|------|
| 画布缩放 / 平移 | 滚轮 / 中键拖拽（或空格+左键） |
| 选中素材后放置 | 左键（按住拖动连刷；+Shift=单块精放） |
| 矩形填充 | Ctrl+左键拖框（+Shift=跳过已有内容） |
| 油漆桶（连通区域整体替换） | G 进入/退出，左键点击填充 |
| 吸取画布上的素材 | 右键 |
| 直线工具 | L（点起点，再点终点） |
| 橡皮擦（只清当前层） | E 进入/退出，左键或拖动擦除 |
| 选择模式 | S（拖框选/单击选/拖动移动，格与物件一起选） |
| 全选 / 复制 / 粘贴 / 删除 / 镜像 | 选择模式下 Ctrl+A / Ctrl+C / Ctrl+V / Delete / H |
| 预制件（组合保存/复放） | 选择内容后 Ctrl+P 保存，P 放置到鼠标处 |
| 批量替换（指着什么换什么） | R：鼠标下素材全图换为选中素材 |
| 素材标签 | T：给选中素材打标签（面板出「#标签」分类） |
| 导出为运行时场景 + JSON | Ctrl+E（导出前自动跑地图检查） |
| 撤销 / 重做（区域操作整段撤销） | Ctrl+Z / Ctrl+Y |
| 保存 / 另存为 / 打开 | Ctrl+S / Ctrl+Shift+S / Ctrl+O（90 秒自动保存，启动自动恢复） |
| 收藏素材 / 聚焦选中 | F 收藏当前素材（面板「★收藏」分类）/ Q 相机聚焦 |
| 快捷栏 1-9 | 数字键或点击底部快捷栏 |
| 素材面板搜索 / 显隐 | 面板顶部搜索框 / Tab |
| 地图检查（断路/堵路/孤立区/碰撞重叠） | F9 |
| 连接规则查看（道路/墙体自动变体说明） | F8 |

## 快速开始

### 用户

首个发布版（P0 完成）将提供免安装的可执行程序下载。当前阶段可从源码运行，或等待 Release；仓库自带**演示素材包**（程序生成的占位素材，可开箱试玩全部功能）。

### 开发者 / 贡献者

1. 安装 [Godot 4.6+](https://godotengine.org/download)（标准版即可，无需 .NET）。
2. 克隆仓库：

   ```bash
   git clone https://github.com/Xeehho/tilemason.git
   ```

3. 用 Godot 打开项目根目录的 `project.godot`，等待导入完成。
4. 按 **F5** 运行：滚轮缩放画布，中键（或空格+左键）拖拽平移。

### 发布流程（维护者）

推送 `v*` 标签即触发机器人自动构建并发布：

```bash
git tag v0.1.0
git push origin v0.1.0
```

CI（GitHub Actions）会自动导出 Windows / Linux / macOS 免安装包并挂到 GitHub Release。也可在 Actions 页手动触发 **Release** 工作流做一次试构建（产物在该次运行页下载，不会发布）。

## 素材约定

- 仓库自带 `assets/demo/` **演示素材包**（程序生成的自绘占位素材，CC0），开箱即可试玩；你自己的素材包放入 `assets/packs/<包名>/`，格式见 [assets/packs/README.md](assets/packs/README.md)（`pack.json` 清单 + `tiles/`、`props/` 目录）。
- 默认约定：**16px 纹理网格 + 48px 组装块**（源自实际像素城市项目的验证经验），同时支持其他网格尺寸。
- 建筑与道具使用**底边中心锚点**，地面与规则方块使用左上角锚点——这是 y-sort 遮挡正确的关键。

## 目录结构

```text
tilemason/
├── docs/design.md      # 设计基准文档
├── src/
│   ├── main.gd         # 编辑器外壳：装配与工具路由（放置/选择/橡皮擦/直线/矩形）
│   ├── core/           # 数据层：地图文档 / 命令栈 / 选区 / 素材库 / 地图检查
│   ├── canvas/         # 画布：相机 / 网格 / 地图视图 / 预览
│   ├── ui/             # 面板：素材库 / 图层
│   └── assets/         # 素材库数据层（扫描/清单/缓存）
├── probes/             # 无人值守验证探针（每次提交前全量重跑）
├── tools/              # 生成工具（演示素材包生成器等）
├── assets/demo/        # 演示素材包（随仓库分发）
├── assets/packs/       # 本地素材包（不入库）
└── .github/workflows/  # CI：脚本解析检查 + 冒烟启动
```

## 路线图

- **P0 可用画笔**：单块/连续放置、半透明预览、橡皮擦、网格吸附、撤销重做、保存场景
- **P1 区域和图层**：矩形填充、框选/移动/复制、图层显隐/锁定/透明度
- **P2 自动连接**：道路/墙体连接、转角/T形/十字、边缘件、连接规则编辑器
- **P3 生产力**：收藏/最近/标签、快捷栏、预制件、批量替换、碰撞与通路检查
- **P4 运行时接入**：生成 Godot 场景与碰撞、交互锚点、验收截图

## 参与贡献

见 [CONTRIBUTING.md](CONTRIBUTING.md)。提交前请确认 `git status` 干净、CI 绿灯。

## 许可证

[MIT](LICENSE)。仓库不含任何第三方素材；素材包版权归各自作者。
