# TileMason 2026-09-21 迭代验收报告（第二轮）

验收日期：2026-09-21（Asia/Shanghai）

验收基线：`main` / `bb119778f47d3693c0a7538daa71e6b46c490547`

对比上次报告：`a9509c7`（覆盖连接规则查看器、P3 收官补全、P4 导出闭环和找缺口修复）

远端对齐：`origin/main` 与本地 `HEAD` 相同；验收开始前工作区干净

引擎：Godot 4.6.2 stable（Windows，本机路径 `C:\迅雷下载\Godot_v4.6.2-stable_win64.exe`）

## 总结结论

本轮自动化、视觉和导出链路均通过，之前报告中“P3 尚有未完成项、P4 仅预研”的结论已被后续迭代更新：

- 导入、21 个 `src/` 脚本解析、冒烟启动均通过；
- 11 个功能探针共 **243/243** 条断言通过，退出码全为 0，无脚本错误、引擎错误或泄漏；
- 编辑器截图像素取证 **9/9** 通过；
- 全新隔离用户目录执行 P4 `--export-shot`，场景截图和移动验收均通过：CharacterBody2D 132 步移动 88px 到达终点；
- 性能探针通过：5000 格写入/重建 42/32ms，20000 格写入/重建 168/142ms；
- `git diff --check` 通过，`main` 与 `origin/main` 对齐。

因此，本轮可标记为“自动化与导出验收通过，达到可交付候选状态”。仍需用户进行一次真实窗口手工回测，并决定 NPC/任务可用区域语义和发布 tag；本轮未创建 tag。

## 通过项

### 1. 三关与脚本解析

| 步骤 | 结果 | 备注 |
|---|---:|---|
| `--headless --path . --import` | 退出码 0 | 隔离目录完成导入，无错误/泄漏 |
| `src/` 下 21 个 GDScript 的 `--check-only --script` | 21/21 | 含导出、标签、工具栏、预制件面板和连接规则查看器 |
| `--headless --path . --quit-after 5` | 退出码 0 | 冒烟无错误/泄漏 |
| `git diff --check` | 退出码 0 | 无空白错误 |

### 2. 功能探针

| 探针 | 断言 | 退出码 | 结论 |
|---|---:|---:|---|
| `asset_library_probe.gd` | 20/20 | 0 | 通过 |
| `auto_connect_probe.gd` | 23/23 | 0 | 通过 |
| `connection_rules_probe.gd` | 31/31 | 0 | 通过 |
| `map_checker_probe.gd` | 10/10 | 0 | 通过 |
| `map_document_probe.gd` | 65/65 | 0 | 通过 |
| `map_view_probe.gd` | 47/47 | 0 | 通过，生命周期无泄漏 |
| `perf_probe.gd` | 4/4 | 0 | 通过 |
| `prefab_probe.gd` | 10/10 | 0 | 通过 |
| `scene_export_probe.gd` | 20/20 | 0 | 通过 |
| `tag_probe.gd` | 11/11 | 0 | 通过 |
| `version_history_probe.gd` | 2/2 | 0 | 通过 |
| **合计** | **243/243** | **全为 0** | **通过** |

本轮新增或补全的证据包括：

- F8 连接规则只读查看器：分类、掩码、方向箭头和空库分支；
- 预制件浏览面板：列表、统计、选用高亮、删除；
- 标签系统：T 键输入、`#标签` 虚拟分类和用户级持久化；
- 版本历史轮转：当前档 + `map.1..map.5`，`map.6` 清尾；
- 按素材类型擦除、道路遮挡橙色警示、顶部工具栏当前工具高亮、撤销/重做按钮、缩略图锚点角标；
- 形态 A 场景导出、双 TileMapLayer、场景件碰撞、交互锚点、导出前检查和回载；
- P4 导出后可通行性与真实物理移动；
- 5000/20000 格数据侧性能评估。

### 3. 截图与视觉取证

窗口模式在隔离用户目录下生成截图，运行 `verify_screenshot_probe.gd` 得到 **9/9**：

- 编辑器分辨率、建筑分类缩略图和屋顶特征正常；
- 画布网格、道路、建筑屋顶正常；
- 占格范围框可见；
- L 形道路拐角自动成为弯道变体；
- 道路端头自动成为端头变体。

人工查看同一张截图确认：顶部工具栏图标、五层图层面板、预制件区域、九类素材分类（含室内/家具）、分类数量、搜索框、快捷栏、状态栏文件名与鼠标格坐标均可见。没有发现脚本错误、黑屏或明显布局崩坏。

### 4. P4 导出与真实移动

使用全新的隔离用户目录，确保从空文档自动摆样后执行 `--export-shot`：

- 生成 `export_shot.png` 和 `export_shot_after_move.png`；
- 导出运行时场景成功挂载并渲染；
- 物理移动输出：`到达终点，位移 88px（132 步），碰撞挡停=false`；
- 退出码 0，未发现跳过摆样、脚本错误、引擎错误或泄漏。

这与 `scene_export_probe.gd` 的 20 条结构/锚点/碰撞/通路断言相互独立，形成了“导出数据结构 + 真场景渲染 + 物理移动”的三层证据。

## 阶段判断

### P0–P3

P0/P1/P2 的既有证据保持通过；P3 当前已补齐连接规则查看器、预制件浏览面板和标签系统，243 条探针与截图取证通过。此前 `tilemason-p3.md` 中的旧未完成勾选已同步为完成状态。

### P4

场景导出、JSON、建筑碰撞、交互锚点、导出前检查、截图、通路和真实物理移动均通过。唯一未纳入实现的是“NPC/任务可用区域”：设计文档没有定义区域的数据模型、绘制方式和导出语义，当前应保留为用户决策项，不由验收过程自行假设。

### 性能

当前 Sprite 数据侧实测：5000 格写入/信号同步 42ms、全量重建 32ms；20000 格为 168ms/142ms，均在探针阈值内。该结果不等于所有机器上的渲染 FPS 保证；若目标地图超过 5000 格或低端设备出现卡顿，再按任务记录迁移到形态 A 的 TileMapLayer 渲染路径。

## 仍需用户手工回测 / 决策

自动化探针尚未完整模拟桌面输入、对话框选取和跨重启用户体验，建议用临时地图逐项记录截图：

1. F8 连接规则查看器：分类切换、方向文本和关闭/再次打开；
2. T 标签输入、保存、重启后 `#标签` 分类是否仍存在；
3. 预制件面板：选中、放置、删除、撤销/重做；
4. Ctrl+E 导出：文件名、同名覆盖、JSON 与场景文件实际位置；
5. Ctrl+E 前存在断路/堵路/孤立区/碰撞重叠时，是否能看到检查阻止或警告；
6. Ctrl+E 导出后在独立运行时场景中查看遮挡、碰撞和交互锚点；
7. 顶部工具栏点击与快捷键是否一致，当前工具金色高亮是否始终同步；
8. Ctrl+E、Ctrl+S、版本历史打开对话框和 `map.1..map.5` 恢复流程；
9. Ctrl+E/版本历史/按类型擦除等新键位与旧有 E/L/G/R/P/F8/F9 组合是否存在冲突；
10. 由用户决定 NPC/任务可用区域的绘制、保存和导出语义。

## 交付判断

在“自动化和导出证据”层面，本轮已达可交付候选状态；在“用户最终手感与产品语义”层面，仍需完成上面的人工清单和 NPC 区域决策。tag/Release 仍按约定必须由用户明确执行，本轮没有创建或推送 tag。

## 证据与范围说明

- 本轮所有测试均使用隔离临时用户数据目录，未覆盖用户原有地图、收藏、最近使用、预制件或版本历史。
- 截图取证与导出取证分别使用独立隔离目录，避免 `user://map.json` 让摆样逻辑早退。
- 本报告只新增验收文档；功能代码没有因本轮验收而修改。
