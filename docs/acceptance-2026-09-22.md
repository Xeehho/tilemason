# TileMason 迭代验收报告（2026-09-22）

## 结论

本轮 UI 重构增量与现有 P0–P4 链路通过自动化、像素取证和真实窗口回测。未发现新的阻断项；打 tag 发布仍保留为用户决策。

## 自动化证据

- Godot 4.6.2：`--import`、24 个 `src/` GDScript `--check-only --script`、`--quit-after 5` 均通过。
- 17 个非截图探针：429 条断言全部通过，退出码均为 0，无 `SCRIPT ERROR`、`ERROR`、`FAIL` 或泄漏输出。
- 截图取证：1600×900 为 12/12，1280×720 为 9/9；两档均验证 Dock 底色、Divider、素材缩略图、画布网格、道路/建筑与自动连接证据。

## 本轮修复与改进

- 选择工具在未完成 `_ready()` 的互斥切换路径中不再访问空的矩形预览/地图视图。
- UI 回归探针改为自建物件夹具，不再依赖用户存档；修复探针字符串格式化错误和隐藏路径悬挂。
- Dock 折叠手柄增加 1px Divider，解决标准档截图中左侧分隔线不可见。
- Tab 隐藏素材面板时同步释放右 Dock 空间；恢复时同步显示，手动折叠状态保持独立。

## 真实窗口回测

- F1 快捷键帮助浮层：四组中文文本可读。
- F9 检查页：自动跳转、检查结果和“重新检查”入口可见。
- 图层/预制件/检查三页签切换、活动层点名、左右 Dock 折叠与恢复通过。
- Tab 隐藏/恢复素材栏通过，快捷栏 E/1–7 角标和状态栏中文可见。

## 待用户决策

- 是否打 tag 触发发布。
- NPC/任务可用区域的产品语义是否立项。
